# v1.0.0 確認記録

確認日：2026年9月18日（日本時間）

## 基準となる実装

- リポジトリ：<https://github.com/skamatakiwriter-rgb/reservation-tool-next>
- 機能完成時点：`c70be326b0841380b654a00b177c2ad85aa665be`
- 作業開始時、GitHubの公開mainと元のローカルHEADが一致。元作業ツリーは変更なし、既存タグなし。
- v1.0.0ではREADME・紹介資料・画像・バージョン番号のみを変更。機能実装は上記コミットと同じ。

## 照合した資料・実装

既存の「設計最終確認」内の基本設計書（01）、最終受入確認記録（09）、Netlify公開前構成レビュー（10）、公開デモ版完成記録（11）を参照しました。設計原本は既存の保管場所に保持しています。

状態遷移、初期データ、IndexedDBスキーマ、トランザクション・再送識別・版競合、BroadcastChannel、3画面のルーティングを実装と照合しました。今回は全受入項目の再試験ではなく、完成版を紹介・固定するための確認です。

## 今回実行した検査

| 検査 | 結果 |
|---|---|
| `npm ci --offline --no-audit --no-fund` | 成功、lockfileに従い導入 |
| `npm run typecheck` | 合格 |
| `npm run test` | 9ファイル74件合格 |
| `npm run lint` | 終了コード0 |
| `npm run build` | 合格、95モジュール |

Node.js 24.18.0、npm 11.16.0、Windowsで実施。初回のサンドボックス内実行ではViteの子プロセス起動がEPERMとなり、通常権限で再実行して合格しました。

## 公開版の実画面確認

対象：<https://reservation-tool-next-demo.netlify.app/>、Codex in-app browser。

- デモ入口、利用者画面、管理者画面を直接表示。
- 管理画面を開始し、カレンダー、初期予約9件の一覧、DEMO-009の再受付元と履歴を確認。
- 管理画面の再読み込み後も表示を確認。利用者画面も再度直接アクセスしてカテゴリー表示を確認。
- 蛍光管持込を架空情報で入力し、確認画面から申込み、状態「確定」と受付番号の表示を確認。
- 申込み後、管理者の予約一覧に同じ受付番号が表示されることを確認。
- 掲載画像は公開版を撮影。カレンダー・履歴は申込み前、一覧は申込み後にDEMO-で初期予約を検索して撮影。

印刷プレビュー、狭幅、初期化・削除、複数タブの詳細受入は既存の受入記録を参照し、今回新たにすべてを再実行したとは扱いません。ネットワーク全通信の監査や本番運用適合性の保証も対象外です。

## 固定の方針

`package.json` と `package-lock.json` の版を1.0.0へ更新し、資料・画像を含むコミットへ注釈付き `v1.0.0` タグを付けます。タグは付け替えず、後続変更は別コミットと次の版で管理します。Gitタグは完成時点を参照するもので、ホスティングURLそのものを固定する機能ではありません。
